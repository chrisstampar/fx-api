# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Rate limiting
slowapi>=0.1.9
limits>=3.13.0,<4.0.0
packaging>=21.0,<25.0

# HTTP client for price fetching
httpx>=0.25.0

# f(x) Protocol SDK
fx-sdk>=0.3.0

# Note: Development dependencies (pytest, etc.) are in requirements-dev.txt
# This file contains only production dependencies for Vercel deployment

